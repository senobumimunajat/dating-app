const { createTheme } = require("@mui/material");

const theme = createTheme({
    palette: {
        primary: {
            main: '#e60965',
            "400": "#ff5992",
            "900": "#ad003b",
        }
    }
})

export default theme;